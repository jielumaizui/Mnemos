# Mnemos 核心依赖
# Python 3.10+

# 核心依赖
requests>=2.28.0
pyyaml>=6.0
watchdog>=3.0.0
numpy>=1.24.0

# 可选：Claude API（用于蒸馏 Agent）
# anthropic>=0.18.0

# Tavily API（用于影子页面外部验证）
# tavily-python>=0.3.0

# MCP 协议支持
mcp>=1.0.0

# TOML 写入支持
tomli_w>=1.0.0
